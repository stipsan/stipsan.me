language: node_js

node_js:
  - "7"

cache: yarn

jobs:
  include:
    - stage: deploy to production
      script: skip
      deploy:
        provider: script
        before_script: npm install -g now
        script: yarn now
        on:
          branch: master
